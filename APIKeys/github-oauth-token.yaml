id: github-oauth-token

info:
  name: GitHub OAuth Token
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "gho_[0-9a-zA-Z]{36}"
